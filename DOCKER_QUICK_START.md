# Docker 管理系统 - 快速开始指南

## ⚡ 5 分钟快速上手

### 1️⃣ 启动应用

```bash
# 在项目根目录执行
npm run dev

# 等待显示类似信息
# ➜  Local:   http://localhost:5173/
# ➜  Network: use --host to expose
```

### 2️⃣ 打开浏览器

访问：**http://localhost:5173**

### 3️⃣ 导航到 Docker 管理

在左侧菜单找到 **🐳 Docker 管理** 或在标签页中选择

---

## 🎯 核心功能速查

### 容器操作

| 操作 | 按钮 | 说明 |
|------|------|------|
| **进入容器** | 💻 | 仅对运行中的容器显示，点击打开内置终端或获取命令 |
| **启动容器** | ▶ | 启动已停止的容器 |
| **停止容器** | ⏹ | 停止运行中的容器 |
| **删除容器** | 🗑 | 删除容器（支持强制删除） |

### 镜像操作

| 操作 | 按钮 | 说明 |
|------|------|------|
| **删除镜像** | 🗑 | 删除不需要的镜像 |

### 页面控制

| 操作 | 按钮 | 说明 |
|------|------|------|
| **手动刷新** | 🔄 刷新 | 立即获取最新数据 |
| **自动刷新** | ▶ 自动刷新 | 每 5 秒自动刷新 |
| **搜索** | 🔍 搜索框 | 按容器名或镜像名过滤 |

---

## 💻 使用示例

### 启动一个容器

1. 在容器列表中找到目标容器（状态为 ⭕ 已停止）
2. 点击 **▶ 启动** 按钮
3. 容器状态立即变为 🟢 运行中

### 进入容器执行命令

#### 方式 1：内置终端（推荐）
1. 找到运行中的容器（🟢）
2. 点击 **💻 进入** 按钮
3. 自动打开内置终端
4. 在终端输入命令，例如：`ls -al`, `pwd`, `echo hello`
5. 按 Enter 执行
6. 使用 **上/下箭头键** 浏览命令历史

#### 方式 2：复制命令到本地终端
1. 点击 **💻 进入** 按钮
2. 在对话框中选择你要的 Shell
3. 点击 **复制** 按钮
4. 在你的本地终端粘贴执行

### 删除一个容器

1. 找到要删除的容器
2. 点击 **🗑 删除** 按钮
3. 在确认对话框中点击 **确定**
4. 容器从列表中删除

### 删除一个镜像

1. 切换到 **📦 镜像** 标签页
2. 找到要删除的镜像
3. 点击 **🗑 删除** 按钮
4. 确认删除

---

## 🔄 数据刷新

### 手动刷新
- 点击 **🔄 刷新** 按钮
- 立即加载最新数据

### 自动刷新
- 点击 **▶ 自动刷新** 按钮启用
- 每 5 秒自动刷新一次
- 再次点击关闭

---

## 📊 信息显示

### 容器列表显示的信息

- **容器名** - 容器的识别名称
- **镜像** - 该容器使用的镜像
- **状态** - 🟢 运行中 / ⭕ 已停止
- **端口映射** - 对外暴露的端口
- **网络** - 连接的网络
- **CPU 使用** - 实时 CPU 使用百分比
- **内存使用** - 实时内存使用量和限制
- **创建时间** - 容器创建时间
- **启动时间** - 最后启动时间

### 镜像列表显示的信息

- **镜像名** - 仓库名:标签
- **大小** - 镜像大小
- **创建时间** - 镜像创建时间
- **使用者** - 有多少容器使用此镜像

---

## 🌐 局域网访问

### 使用 Tailscale

如果你的电脑安装了 Tailscale 并已连接：

```
http://macdemac-mini.taileeb849.ts.net:5173
```

### 使用本地 IP

获取你的本地 IP：
```bash
ifconfig | grep "inet "
```

然后访问（假设 IP 是 192.168.1.100）：
```
http://192.168.1.100:5173
```

> 注意：需要将 IP 添加到 `vite.config.js` 的 `allowedHosts`

---

## 🐛 常见问题

### Q: 容器操作后列表没有更新？

**A:**
1. 点击 🔄 刷新 手动刷新
2. 或启用 ▶ 自动刷新 实时更新

### Q: 进入容器没有响应？

**A:**
1. 确保容器是运行中的（🟢）
2. 检查浏览器控制台是否有错误
3. 尝试重新点击 💻 进入

### Q: 删除镜像失败？

**A:**
1. 镜像可能有容器在使用
2. 先删除使用该镜像的所有容器
3. 然后再删除镜像

### Q: 无法从其他电脑访问？

**A:**
1. 确保 Tailscale 已连接：`tailscale status`
2. 检查防火墙设置
3. 在 CORS 白名单中添加你的主机名

---

## 📱 移动设备访问

### 从 iPhone/iPad

1. 安装 Tailscale 应用
2. 连接到 Tailscale 网络
3. 在浏览器访问：`http://macdemac-mini.taileeb849.ts.net:5173`

### 从 Android

同样的步骤，安装 Tailscale 后即可访问

---

## ⌨️ 终端快捷键

在内置终端中：

| 快捷键 | 功能 |
|--------|------|
| **↑ 上箭头** | 查看前一条命令 |
| **↓ 下箭头** | 查看后一条命令 |
| **Enter** | 执行命令 |
| **Ctrl+C** | 中断执行 |

### 内置命令

```
help    - 显示帮助信息
clear   - 清屏
exit    - 关闭终端
```

---

## 🎨 界面说明

### 状态指示器

- 🟢 **绿色** - 容器运行中
- ⭕ **灰色** - 容器已停止
- 🐳 **鲸鱼** - Docker 正常
- 📦 **包装** - 镜像
- 🌐 **地球** - 网络
- 💾 **磁盘** - 卷

### 标签页

- **🐳 容器** - 容器管理
- **📦 镜像** - 镜像管理
- **🌐 网络** - 网络配置
- **💾 卷** - 数据卷
- **ℹ️ 信息** - Docker 系统信息

---

## 🚀 高级用法

### 执行复杂命令

在终端中执行多行或复杂命令：

```bash
# 查看文件内容
cat /etc/hostname

# 检查网络
ping -c 1 google.com

# 查看进程
ps aux | grep node

# 安装软件（如果有 apt）
apt update && apt install -y vim
```

### 文件操作

```bash
# 创建文件
echo "hello" > test.txt

# 查看文件
cat test.txt

# 删除文件
rm test.txt

# 创建目录
mkdir mydir

# 进入目录
cd mydir
```

---

## 📞 获取帮助

### 查看文档

- **DOCKER_MANAGEMENT_FEATURES.md** - 完整功能说明
- **DOCKER_INTEGRATION_GUIDE.md** - 技术集成指南
- **NETWORK_ACCESS_GUIDE.md** - 网络配置指南

### 检查日志

打开浏览器开发者工具（F12）查看：
- Console 标签 - 应用日志
- Network 标签 - API 请求
- Elements 标签 - HTML 结构

---

## ✅ 检查清单

启动应用前，确保：

- [ ] Node.js 已安装（v16+）
- [ ] Docker 已运行：`docker ps`
- [ ] npm 依赖已安装：`npm install`
- [ ] 端口 5173 和 4000 未被占用

---

## 🎉 你已准备好了！

现在你可以：
1. ✅ 启动和停止容器
2. ✅ 删除镜像和容器
3. ✅ 进入容器执行命令
4. ✅ 实时监控资源使用
5. ✅ 从任何设备访问（使用 Tailscale）

**开始探索吧！** 🚀

---

**更新日期：** 2026-02-24
