# 流程预览界面改进快速指南

> 5 分钟快速了解所有新功能

---

## 🎯 三个核心改进

### 1️⃣ 滚动查看所有步骤（解决：步骤多了就不显示）

**之前**：添加超过 3 个步骤，看不全
**现在**：自动显示滚动条，支持无限数量步骤

```
流程管理 → 选择流程 → 点击"查看步骤"
↓
侧边栏显示步骤列表，如果步骤很多：
  • 列表自动显示滚动条
  • 滚动查看所有步骤
  • 滚动条样式与主题色匹配
```

---

### 2️⃣ 清晰的字段标签和说明（解决：不能添加关键词说明）

**之前**：只显示内容，不知道是什么字段
**现在**：每个字段都有标签和说明文本

```
每个步骤显示：

✓ 步骤名称（粗体）

✓ 步骤描述           ← 清晰的标签
  说明这个步骤的内容和目的  ← 灰色说明文字
  （实际描述内容）

✓ 负责人：XXX        ← 加粗字段名
✓ 预计耗时：2h       ← 加粗字段名
✓ 条件触发           ← 彩色标签
```

---

### 3️⃣ 直接调整步骤顺序（解决：上下顺序不能调整）

**之前**：需要进入编辑器才能调整顺序
**现在**：直接在预览界面调整，一键保存

```
每个步骤右侧显示两个按钮：

  ⬆️ 上移  → 与上一步交换位置
  ⬇️ 下移  → 与下一步交换位置

点击后：
  • 自动交换顺序
  • 自动保存到数据库
  • 页面即时更新
```

---

## 📸 操作演示

### 场景 1：查看多步骤流程

```
1. 点击"查看步骤"打开预览
2. 看到步骤列表，右侧有滚动条
3. 向下滚动，查看更多步骤
4. 每个步骤下方显示清晰的字段说明
```

### 场景 2：调整步骤顺序

```
1. 在预览界面找到要移动的步骤
2. 点击该步骤右侧的 ⬆️ 或 ⬇️ 按钮
3. 步骤立即交换位置
4. 自动保存到数据库（无需手动保存）
5. 关闭预览，重新打开，顺序保持不变 ✓
```

### 场景 3：从预览跳转到编辑器

```
1. 点击"查看步骤"打开预览
2. 发现需要更复杂的编辑（添加/删除步骤等）
3. 点击"编辑流程"按钮
4. 直接跳转到 FlowDiagramEditor
5. 完成编辑后返回，预览自动更新
```

---

## ⚙️ 技术细节

### 滚动容器实现
```css
.flow-detail__steps {
  max-height: 60vh;      /* 限制高度 */
  overflow-y: auto;      /* 启用滚动 */
  padding-right: 8px;    /* 为滚动条预留空间 */
}

/* 自定义滚动条 */
.flow-detail__steps::-webkit-scrollbar {
  width: 6px;
}
```

### 字段标签实现
```html
<div class="step__field">
  <label class="step__field-label">
    <span class="field-label-text">步骤描述</span>
    <span class="field-label-hint">说明这个步骤的内容和目的</span>
  </label>
  <p class="step__desc">{{ step.description }}</p>
</div>
```

### 步骤排序实现
```javascript
moveStepInPreview(index, direction) {
  // 1. 验证操作有效性
  // 2. 交换内存中的步骤顺序
  // 3. 调用 API 保存到数据库
  // 4. 同步更新本地数据
  // 5. 显示成功消息
}
```

---

## ✅ 验证清单

使用新功能时，你可以验证以下功能是否正常：

### 滚动功能
- [ ] 创建包含 10+ 个步骤的流程
- [ ] 打开预览，右侧显示滚动条
- [ ] 上下滚动，能查看所有步骤
- [ ] 滚动条颜色与主题匹配

### 字段标签
- [ ] 查看步骤，看到"步骤描述"标签
- [ ] 标签下有灰色说明文字
- [ ] 字段名称加粗（负责人、预计耗时）
- [ ] "条件触发"显示为彩色标签

### 步骤排序
- [ ] 找到某个步骤，右侧有 ⬆️/⬇️ 按钮
- [ ] 点击 ⬆️，步骤与上一步交换位置
- [ ] 点击 ⬇️，步骤与下一步交换位置
- [ ] 第一步的 ⬆️ 按钮禁用（灰色）
- [ ] 最后一步的 ⬇️ 按钮禁用（灰色）
- [ ] 刷新页面，顺序保持不变

### 编辑按钮
- [ ] 预览界面右上角有"编辑流程"按钮
- [ ] 点击后跳转到 FlowDiagramEditor
- [ ] 可以进行完整的流程编辑

---

## 🔧 常见问题

### Q: 为什么某个步骤的上移按钮是灰色的？
**A**: 这是第一个步骤，它上面没有步骤，所以上移按钮被禁用。同样，最后一个步骤的下移按钮也被禁用。

### Q: 点击移动按钮后没有反应怎么办？
**A**: 可能是网络问题。检查：
1. 网络连接是否正常
2. 打开浏览器控制台（F12），查看是否有错误信息
3. 刷新页面重试

### Q: 调整顺序后刷新页面，顺序复原了怎么办？
**A**: 说明 API 调用失败（但没有显示错误）。请检查后端服务是否正常运行。

### Q: 如何在移动端查看多个步骤？
**A**: 移动设备上滚动条仍然可用，只是显示方式可能不同。向上/向下滑动列表即可查看所有步骤。

---

## 💡 使用建议

### ✅ 推荐做法
1. **快速查看**：使用预览界面查看步骤列表和说明
2. **调整顺序**：直接在预览界面使用 ⬆️/⬇️ 按钮
3. **复杂编辑**：点击"编辑流程"进入编辑器，添加/删除步骤
4. **完整流程**：预览 → 快速调整 → 编辑 → 预览

### ⚠️ 注意事项
1. 移动步骤后会立即保存，无法撤销（后续版本可能支持）
2. 如果同时有多人编辑同一个流程，可能出现冲突（需要刷新查看最新数据）
3. 在预览界面调整顺序比较快，但编辑器有更多功能

---

## 🚀 下一步

### 已完成的改进
- ✅ 滚动支持
- ✅ 字段标签
- ✅ 步骤排序
- ✅ 编辑快捷

### 计划的改进
- ⏳ 拖拽排序（替代上下按钮）
- ⏳ Toast 通知（替代 console.log）
- ⏳ 撤销/重做
- ⏳ 批量操作

---

## 📚 详细文档

想了解更多技术细节，请查看：

- **FLOW_PREVIEW_IMPROVEMENTS.md** - 完整的改进说明（617 行）
- **FLOW_EDITOR_IMPROVEMENTS.md** - 编辑器改进说明
- **FLOW_MANAGEMENT_SETUP.md** - 流程管理完整指南

---

**版本**：1.0
**完成日期**：2026年2月26日
**最后更新**：2026年2月26日
