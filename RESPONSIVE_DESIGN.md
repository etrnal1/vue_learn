# 📱 ITSM 系统响应式设计文档

## 🎯 适配目标

| 设备类型 | 屏幕宽度 | 特点 |
|---------|---------|------|
| **手机** | < 480px | 单列布局，堆叠式菜单 |
| **小平板** | 480px - 768px | 两列网格，优化触屏交互 |
| **平板** | 768px - 1024px | 三列网格，混合布局 |
| **桌面** | > 1024px | 完整布局，所有功能可见 |

---

## 🔧 响应式断点

```css
/* 平板及以上 */
@media (max-width: 1024px) { }

/* 平板和手机 */
@media (max-width: 768px) { }

/* 手机 */
@media (max-width: 480px) { }
```

---

## 📊 各模块适配情况

### **1️⃣ 代码编辑器 (CodePlayground)**

| 屏幕 | 布局 | 编辑器高度 | 操作栏 |
|-----|------|---------|--------|
| 桌面 | 左右分割 | 400px | 水平排列 |
| 平板 | 上下堆叠 | 300px | 水平排列 |
| 手机 | 全宽堆叠 | 250px/200px | 自适应换行 |

**关键改进：**
- ✅ 编辑器和执行结果从并排改为上下堆叠
- ✅ 按钮自动缩小和换行
- ✅ 代码片段列表优化高度

### **2️⃣ 事件管理 (IncidentSection)**

| 屏幕 | 工单卡片网格 | 按钮宽度 | 详情页 |
|-----|-----------|---------|--------|
| 桌面 | 3列自动填充 | 固定宽度 | 模态框 |
| 平板 | 1-2列 | 自适应 | 全宽优化 |
| 手机 | 单列 | 100%宽度 | 底部弹出 |

**关键改进：**
- ✅ 网格列数自动调整
- ✅ 按钮自动伸缩
- ✅ 详情页填充整个屏幕

### **3️⃣ 服务请求 (ServiceRequest)**

| 屏幕 | 服务目录 | 请求卡片 | 操作按钮 |
|-----|---------|---------|---------|
| 桌面 | 4列网格 | 3列 | 分散排列 |
| 平板 | 2列 | 1列 | 换行显示 |
| 手机 | 单列 | 单列 | 堆叠排列 |

**关键改进：**
- ✅ 目录和列表自动调整列数
- ✅ 审批操作按钮自适应宽度

### **4️⃣ 知识库 (KnowledgeBase)**

| 屏幕 | 文章网格 | 内容区域 | 标签 |
|-----|---------|---------|------|
| 桌面 | 3列 | 宽敞 | 多个一行 |
| 平板 | 2列 | 适中 | 换行显示 |
| 手机 | 单列 | 紧凑 | 堆叠显示 |

**关键改进：**
- ✅ 文章卡片自动缩小
- ✅ 标签自动换行

### **5️⃣ 流程管理 (ProcessFlow)**

| 屏幕 | 流程卡片 | 时间线 | 步骤编辑 |
|-----|---------|--------|---------|
| 桌面 | 2列网格 | 完整显示 | 水平排列 |
| 平板 | 单列 | 简化显示 | 垂直排列 |
| 手机 | 单列 | 精简显示 | 堆叠排列 |

**关键改进：**
- ✅ 时间线节点自动缩小
- ✅ 步骤操作按钮优化

### **6️⃣ 仪表板 (Dashboard)**

| 屏幕 | 统计卡片 | 快速操作 | 列表 |
|-----|---------|---------|------|
| 桌面 | 6列 | 3列 | 完整显示 |
| 平板 | 3列 | 全宽 | 优化显示 |
| 手机 | 2列/单列 | 全宽堆叠 | 单列显示 |

**关键改进：**
- ✅ 统计卡片自动调整数量
- ✅ 快速操作按钮全宽

---

## 📐 具体调整细节

### **字体大小适配**

```css
/* 桌面 */
h2 { font-size: 1.4em; }
button { font-size: 0.9em; }

/* 平板 */
@media (max-width: 768px) {
  h2 { font-size: 1.1em; }
  button { font-size: 0.85em; }
}

/* 手机 */
@media (max-width: 480px) {
  h2 { font-size: 1em; }
  button { font-size: 0.75em; }
}
```

### **间距优化**

```css
/* 桌面 */
gap: 20px; padding: 22px;

/* 平板 */
@media (max-width: 768px) {
  gap: 12px; padding: 16px;
}

/* 手机 */
@media (max-width: 480px) {
  gap: 8px; padding: 12px;
}
```

### **容器宽度**

```css
/* 桌面 */
.modal-large { max-width: 900px; }

/* 平板/手机 */
@media (max-width: 768px) {
  .modal-small, .modal-medium, .modal-large { max-width: 100%; }
}

/* 手机底部弹出 */
@media (max-width: 480px) {
  .modal {
    width: 100%;
    border-radius: 8px 8px 0 0; /* 顶部弧形 */
    max-height: 100vh;
  }
}
```

---

## 🎨 交互优化

### **触屏友好性**

```css
/* 增大触达区域 */
button {
  min-width: 40px;
  min-height: 40px;
  padding: 8px 12px;
}

/* 手机上全宽按钮 */
@media (max-width: 768px) {
  .action-btn {
    width: 100%;
    padding: 10px 12px;
  }
}

/* 防止 iOS 自动缩放 */
input {
  font-size: 16px;
}
```

### **溶通滚动**

```css
/* Tab 导航水平滚动 */
.itsm-sub-tabs {
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
}

/* 列表垂直滚动 */
.snippets-list {
  max-height: 300px;
  overflow-y: auto;
  -webkit-overflow-scrolling: touch;
}
```

### **灵活布局**

```css
/* 桌面：固定网格 */
grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));

/* 平板：2列 */
@media (max-width: 1024px) {
  grid-template-columns: repeat(2, 1fr);
}

/* 手机：1列 */
@media (max-width: 768px) {
  grid-template-columns: 1fr;
}
```

---

## ✅ 测试清单

### **手机 (< 480px)**
- [ ] 所有按钮可点击（最小 40x40px）
- [ ] 文本可读（最小 16px）
- [ ] 模态框全屏显示
- [ ] 菜单可横向滚动
- [ ] 表单输入易操作

### **平板 (480px - 768px)**
- [ ] 2-3列网格显示
- [ ] Tab 导航换行合理
- [ ] 按钮和输入框适中
- [ ] 模态框宽度适当

### **桌面 (> 1024px)**
- [ ] 全功能显示
- [ ] 3+列网格
- [ ] 侧边栏布局
- [ ] 完整的交互反馈

---

## 🚀 使用方法

### **实时测试**

1. 打开浏览器开发者工具 (F12)
2. 点击"切换设备工具栏" (Cmd+Shift+M on Mac)
3. 选择不同设备预设
4. 测试各功能模块

### **常用设备尺寸**

```
iPhone SE: 375px
iPhone 12/13: 390px
iPad Mini: 768px
iPad Pro: 1024px
MacBook: 1440px+
```

---

## 💡 最佳实践

✅ **移动优先设计** - 从小屏幕开始，逐步增加功能
✅ **灵活布局** - 使用 flexbox 和 grid
✅ **相对单位** - em、rem、百分比而非固定 px
✅ **触屏优化** - 按钮最小 40x40px
✅ **字体大小** - 手机最小 14px，避免自动缩放
✅ **图片响应式** - 使用 max-width: 100%
✅ **测试真机** - 不仅在浏览器模拟器中测试

---

## 📝 更新日志

- **v1.0** - 初版响应式设计
  - 3 个断点优化
  - 所有模块适配
  - 代码编辑器优化
  - 触屏友好

---

**最后更新**: 2024年
**维护者**: Claude
**许可证**: MIT
